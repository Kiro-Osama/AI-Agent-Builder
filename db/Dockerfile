# ============================================
# PostgreSQL + pgvector (schema + seeds via Alembic at API startup)
# ============================================
FROM pgvector/pgvector:pg16

EXPOSE 5432
